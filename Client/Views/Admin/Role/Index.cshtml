@{
    ViewData["Title"] = "Role";
}

<h1>Index</h1>
<button class="btn btn-primary" onclick="addModalRole()" data-toggle="modal" data-target="#modalRole">Create New</button>

<table class="table table-striped table-dark" id="dataTablesRole">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody id="tableBody">
    </tbody>
</table>

<div class="modal fade" id="modalRole" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content" id="modalContentRole">
        </div>
    </div>
</div>
